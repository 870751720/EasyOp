# This file was automatically generated by SWIG (https://www.swig.org).
# Version 4.4.1
#
# Do not make changes to this file unless you know what you are doing - modify
# the SWIG interface file instead.

from sys import version_info as _swig_python_version_info
# Import the low-level C/C++ module
if getattr(globals().get("__spec__"), "parent", None) or __package__ or "." in __name__:
    from . import _easyop
else:
    import _easyop

try:
    import builtins as __builtin__
except ImportError:
    import __builtin__

def _swig_repr(self):
    try:
        strthis = "proxy of " + self.this.__repr__()
    except __builtin__.Exception:
        strthis = ""
    return "<%s.%s; %s >" % (self.__class__.__module__, self.__class__.__name__, strthis,)


def _swig_setattr_nondynamic_instance_variable(set):
    def set_instance_attr(self, name, value):
        if name == "this":
            set(self, name, value)
        elif name == "thisown":
            self.this.own(value)
        elif hasattr(self, name) and isinstance(getattr(type(self), name), property):
            set(self, name, value)
        else:
            raise AttributeError("You cannot add instance attributes to %s" % self)
    return set_instance_attr


def _swig_setattr_nondynamic_class_variable(set):
    def set_class_attr(cls, name, value):
        if hasattr(cls, name) and not isinstance(getattr(cls, name), property):
            set(cls, name, value)
        else:
            raise AttributeError("You cannot add class attributes to %s" % cls)
    return set_class_attr


def _swig_add_metaclass(metaclass):
    """Class decorator for adding a metaclass to a SWIG wrapped class - a slimmed down version of six.add_metaclass"""
    def wrapper(cls):
        return metaclass(cls.__name__, cls.__bases__, cls.__dict__.copy())
    return wrapper


class _SwigNonDynamicMeta(type):
    """Meta class to enforce nondynamic attributes (no new attributes) for a class"""
    __setattr__ = _swig_setattr_nondynamic_class_variable(type.__setattr__)


class libop(object):
    thisown = property(lambda x: x.this.own(), lambda x, v: x.this.own(v), doc="The membership flag")
    __repr__ = _swig_repr

    def __init__(self):
        _easyop.libop_swiginit(self, _easyop.new_libop())
    __swig_destroy__ = _easyop.delete_libop

    def Ver(self):
        return _easyop.libop_Ver(self)

    def SetPath(self, path):
        return _easyop.libop_SetPath(self, path)

    def GetPath(self):
        return _easyop.libop_GetPath(self)

    def GetBasePath(self):
        return _easyop.libop_GetBasePath(self)

    def GetID(self):
        return _easyop.libop_GetID(self)

    def GetLastError(self):
        return _easyop.libop_GetLastError(self)

    def SetShowErrorMsg(self, show_type):
        return _easyop.libop_SetShowErrorMsg(self, show_type)

    def Sleep(self, millseconds):
        return _easyop.libop_Sleep(self, millseconds)

    def InjectDll(self, process_name, dll_name):
        return _easyop.libop_InjectDll(self, process_name, dll_name)

    def EnablePicCache(self, enable):
        return _easyop.libop_EnablePicCache(self, enable)

    def CapturePre(self, file_name):
        return _easyop.libop_CapturePre(self, file_name)

    def SetScreenDataMode(self, mode):
        return _easyop.libop_SetScreenDataMode(self, mode)

    def AStarFindPath(self, mapWidth, mapHeight, disable_points, beginX, beginY, endX, endY):
        return _easyop.libop_AStarFindPath(self, mapWidth, mapHeight, disable_points, beginX, beginY, endX, endY)

    def FindNearestPos(self, all_pos, type, x, y):
        return _easyop.libop_FindNearestPos(self, all_pos, type, x, y)

    def EnumWindow(self, parent, title, class_name, filter):
        return _easyop.libop_EnumWindow(self, parent, title, class_name, filter)

    def EnumWindowByProcess(self, process_name, title, class_name, filter):
        return _easyop.libop_EnumWindowByProcess(self, process_name, title, class_name, filter)

    def EnumProcess(self, name):
        return _easyop.libop_EnumProcess(self, name)

    def ClientToScreen(self, ClientToScreen):
        return _easyop.libop_ClientToScreen(self, ClientToScreen)

    def FindWindow(self, class_name, title):
        return _easyop.libop_FindWindow(self, class_name, title)

    def FindWindowByProcess(self, process_name, class_name, title):
        return _easyop.libop_FindWindowByProcess(self, process_name, class_name, title)

    def FindWindowByProcessId(self, process_id, class_name, title):
        return _easyop.libop_FindWindowByProcessId(self, process_id, class_name, title)

    def FindWindowEx(self, parent, class_name, title):
        return _easyop.libop_FindWindowEx(self, parent, class_name, title)

    def GetClientRect(self, hwnd):
        return _easyop.libop_GetClientRect(self, hwnd)

    def GetClientSize(self, hwnd):
        return _easyop.libop_GetClientSize(self, hwnd)

    def GetForegroundFocus(self):
        return _easyop.libop_GetForegroundFocus(self)

    def GetForegroundWindow(self):
        return _easyop.libop_GetForegroundWindow(self)

    def GetMousePointWindow(self):
        return _easyop.libop_GetMousePointWindow(self)

    def GetPointWindow(self, x, y):
        return _easyop.libop_GetPointWindow(self, x, y)

    def GetProcessInfo(self, pid):
        return _easyop.libop_GetProcessInfo(self, pid)

    def GetSpecialWindow(self, flag):
        return _easyop.libop_GetSpecialWindow(self, flag)

    def GetWindow(self, hwnd, flag):
        return _easyop.libop_GetWindow(self, hwnd, flag)

    def GetWindowClass(self, hwnd):
        return _easyop.libop_GetWindowClass(self, hwnd)

    def GetWindowProcessId(self, hwnd):
        return _easyop.libop_GetWindowProcessId(self, hwnd)

    def GetWindowProcessPath(self, hwnd):
        return _easyop.libop_GetWindowProcessPath(self, hwnd)

    def GetWindowRect(self, hwnd):
        return _easyop.libop_GetWindowRect(self, hwnd)

    def GetWindowState(self, hwnd, flag):
        return _easyop.libop_GetWindowState(self, hwnd, flag)

    def GetWindowTitle(self, hwnd):
        return _easyop.libop_GetWindowTitle(self, hwnd)

    def MoveWindow(self, hwnd, x, y):
        return _easyop.libop_MoveWindow(self, hwnd, x, y)

    def ScreenToClient(self, hwnd):
        return _easyop.libop_ScreenToClient(self, hwnd)

    def SendPaste(self, hwnd):
        return _easyop.libop_SendPaste(self, hwnd)

    def SetClientSize(self, hwnd, width, hight):
        return _easyop.libop_SetClientSize(self, hwnd, width, hight)

    def SetWindowState(self, hwnd, flag):
        return _easyop.libop_SetWindowState(self, hwnd, flag)

    def SetWindowSize(self, hwnd, width, height):
        return _easyop.libop_SetWindowSize(self, hwnd, width, height)

    def SetWindowText(self, hwnd, title):
        return _easyop.libop_SetWindowText(self, hwnd, title)

    def SetWindowTransparent(self, hwnd, trans):
        return _easyop.libop_SetWindowTransparent(self, hwnd, trans)

    def SendString(self, hwnd, str):
        return _easyop.libop_SendString(self, hwnd, str)

    def SendStringIme(self, hwnd, str):
        return _easyop.libop_SendStringIme(self, hwnd, str)

    def RunApp(self, cmdline, mode):
        return _easyop.libop_RunApp(self, cmdline, mode)

    def WinExec(self, cmdline, cmdshow):
        return _easyop.libop_WinExec(self, cmdline, cmdshow)

    def GetCmdStr(self, cmd, millseconds):
        return _easyop.libop_GetCmdStr(self, cmd, millseconds)

    def SetClipboard(self, str):
        return _easyop.libop_SetClipboard(self, str)

    def GetClipboard(self):
        return _easyop.libop_GetClipboard(self)

    def Delay(self, mis):
        return _easyop.libop_Delay(self, mis)

    def Delays(self, mis_min, mis_max):
        return _easyop.libop_Delays(self, mis_min, mis_max)

    def BindWindow(self, hwnd, display, mouse, keypad, mode):
        return _easyop.libop_BindWindow(self, hwnd, display, mouse, keypad, mode)

    def UnBindWindow(self):
        return _easyop.libop_UnBindWindow(self)

    def GetBindWindow(self):
        return _easyop.libop_GetBindWindow(self)

    def IsBind(self):
        return _easyop.libop_IsBind(self)

    def GetCursorPos(self):
        return _easyop.libop_GetCursorPos(self)

    def MoveR(self, x, y):
        return _easyop.libop_MoveR(self, x, y)

    def MoveTo(self, x, y):
        return _easyop.libop_MoveTo(self, x, y)

    def MoveToEx(self, x, y, w, h):
        return _easyop.libop_MoveToEx(self, x, y, w, h)

    def LeftClick(self):
        return _easyop.libop_LeftClick(self)

    def LeftDoubleClick(self):
        return _easyop.libop_LeftDoubleClick(self)

    def LeftDown(self):
        return _easyop.libop_LeftDown(self)

    def LeftUp(self):
        return _easyop.libop_LeftUp(self)

    def MiddleClick(self):
        return _easyop.libop_MiddleClick(self)

    def MiddleDown(self):
        return _easyop.libop_MiddleDown(self)

    def MiddleUp(self):
        return _easyop.libop_MiddleUp(self)

    def RightClick(self):
        return _easyop.libop_RightClick(self)

    def RightDown(self):
        return _easyop.libop_RightDown(self)

    def RightUp(self):
        return _easyop.libop_RightUp(self)

    def WheelDown(self):
        return _easyop.libop_WheelDown(self)

    def WheelUp(self):
        return _easyop.libop_WheelUp(self)

    def SetMouseDelay(self, type, delay):
        return _easyop.libop_SetMouseDelay(self, type, delay)

    def GetKeyState(self, vk_code):
        return _easyop.libop_GetKeyState(self, vk_code)

    def KeyDown(self, vk_code):
        return _easyop.libop_KeyDown(self, vk_code)

    def KeyDownChar(self, vk_code):
        return _easyop.libop_KeyDownChar(self, vk_code)

    def KeyUp(self, vk_code):
        return _easyop.libop_KeyUp(self, vk_code)

    def KeyUpChar(self, vk_code):
        return _easyop.libop_KeyUpChar(self, vk_code)

    def WaitKey(self, vk_code, time_out):
        return _easyop.libop_WaitKey(self, vk_code, time_out)

    def KeyPress(self, vk_code):
        return _easyop.libop_KeyPress(self, vk_code)

    def KeyPressChar(self, vk_code):
        return _easyop.libop_KeyPressChar(self, vk_code)

    def SetKeypadDelay(self, type, delay):
        return _easyop.libop_SetKeypadDelay(self, type, delay)

    def KeyPressStr(self, key_str, delay):
        return _easyop.libop_KeyPressStr(self, key_str, delay)

    def Capture(self, x1, y1, x2, y2, file_name):
        return _easyop.libop_Capture(self, x1, y1, x2, y2, file_name)

    def CmpColor(self, x, y, color, sim):
        return _easyop.libop_CmpColor(self, x, y, color, sim)

    def FindColor(self, x1, y1, x2, y2, color, sim, dir):
        return _easyop.libop_FindColor(self, x1, y1, x2, y2, color, sim, dir)

    def FindColorEx(self, x1, y1, x2, y2, color, sim, dir):
        return _easyop.libop_FindColorEx(self, x1, y1, x2, y2, color, sim, dir)

    def GetColorNum(self, x1, y1, x2, y2, color, sim):
        return _easyop.libop_GetColorNum(self, x1, y1, x2, y2, color, sim)

    def FindMultiColor(self, x1, y1, x2, y2, first_color, offset_color, sim, dir):
        return _easyop.libop_FindMultiColor(self, x1, y1, x2, y2, first_color, offset_color, sim, dir)

    def FindMultiColorEx(self, x1, y1, x2, y2, first_color, offset_color, sim, dir):
        return _easyop.libop_FindMultiColorEx(self, x1, y1, x2, y2, first_color, offset_color, sim, dir)

    def FindPic(self, x1, y1, x2, y2, files, delta_color, sim, dir):
        return _easyop.libop_FindPic(self, x1, y1, x2, y2, files, delta_color, sim, dir)

    def FindPicEx(self, x1, y1, x2, y2, files, delta_color, sim, dir):
        return _easyop.libop_FindPicEx(self, x1, y1, x2, y2, files, delta_color, sim, dir)

    def FindPicExS(self, x1, y1, x2, y2, files, delta_color, sim, dir):
        return _easyop.libop_FindPicExS(self, x1, y1, x2, y2, files, delta_color, sim, dir)

    def FindColorBlock(self, x1, y1, x2, y2, color, sim, count, height, width):
        return _easyop.libop_FindColorBlock(self, x1, y1, x2, y2, color, sim, count, height, width)

    def FindColorBlockEx(self, x1, y1, x2, y2, color, sim, count, height, width):
        return _easyop.libop_FindColorBlockEx(self, x1, y1, x2, y2, color, sim, count, height, width)

    def GetColor(self, x, y):
        return _easyop.libop_GetColor(self, x, y)

    def SetDisplayInput(self, mode):
        return _easyop.libop_SetDisplayInput(self, mode)

    def LoadPic(self, file_name):
        return _easyop.libop_LoadPic(self, file_name)

    def FreePic(self, file_name):
        return _easyop.libop_FreePic(self, file_name)

    def LoadMemPic(self, file_name, data, size):
        return _easyop.libop_LoadMemPic(self, file_name, data, size)

    def GetPicSize(self, pic_name):
        return _easyop.libop_GetPicSize(self, pic_name)

    def GetScreenData(self, x1, y1, x2, y2):
        return _easyop.libop_GetScreenData(self, x1, y1, x2, y2)

    def GetScreenDataBmp(self, x1, y1, x2, y2):
        return _easyop.libop_GetScreenDataBmp(self, x1, y1, x2, y2)

    def GetScreenFrameInfo(self):
        return _easyop.libop_GetScreenFrameInfo(self)

    def MatchPicName(self, pic_name):
        return _easyop.libop_MatchPicName(self, pic_name)

    def SetOcrEngine(self, path_of_engine, dll_name, argv):
        return _easyop.libop_SetOcrEngine(self, path_of_engine, dll_name, argv)

    def SetDict(self, idx, file_name):
        return _easyop.libop_SetDict(self, idx, file_name)

    def GetDict(self, idx, font_index):
        return _easyop.libop_GetDict(self, idx, font_index)

    def SetMemDict(self, idx, data, size):
        return _easyop.libop_SetMemDict(self, idx, data, size)

    def UseDict(self, idx):
        return _easyop.libop_UseDict(self, idx)

    def AddDict(self, idx, dict_info):
        return _easyop.libop_AddDict(self, idx, dict_info)

    def SaveDict(self, idx, file_name):
        return _easyop.libop_SaveDict(self, idx, file_name)

    def ClearDict(self, idx):
        return _easyop.libop_ClearDict(self, idx)

    def GetDictCount(self, idx):
        return _easyop.libop_GetDictCount(self, idx)

    def GetNowDict(self):
        return _easyop.libop_GetNowDict(self)

    def FetchWord(self, x1, y1, x2, y2, color, word):
        return _easyop.libop_FetchWord(self, x1, y1, x2, y2, color, word)

    def GetWordsNoDict(self, x1, y1, x2, y2, color):
        return _easyop.libop_GetWordsNoDict(self, x1, y1, x2, y2, color)

    def GetWordResultCount(self, result):
        return _easyop.libop_GetWordResultCount(self, result)

    def GetWordResultPos(self, result, index):
        return _easyop.libop_GetWordResultPos(self, result, index)

    def GetWordResultStr(self, result, index):
        return _easyop.libop_GetWordResultStr(self, result, index)

    def Ocr(self, x1, y1, x2, y2, color, sim):
        return _easyop.libop_Ocr(self, x1, y1, x2, y2, color, sim)

    def OcrEx(self, x1, y1, x2, y2, color, sim):
        return _easyop.libop_OcrEx(self, x1, y1, x2, y2, color, sim)

    def FindStr(self, x1, y1, x2, y2, strs, color, sim):
        return _easyop.libop_FindStr(self, x1, y1, x2, y2, strs, color, sim)

    def FindStrEx(self, x1, y1, x2, y2, strs, color, sim):
        return _easyop.libop_FindStrEx(self, x1, y1, x2, y2, strs, color, sim)

    def OcrAuto(self, x1, y1, x2, y2, sim):
        return _easyop.libop_OcrAuto(self, x1, y1, x2, y2, sim)

    def OcrFromFile(self, file_name, color_format, sim):
        return _easyop.libop_OcrFromFile(self, file_name, color_format, sim)

    def OcrAutoFromFile(self, file_name, sim):
        return _easyop.libop_OcrAutoFromFile(self, file_name, sim)

    def FindLine(self, x1, y1, x2, y2, color, sim):
        return _easyop.libop_FindLine(self, x1, y1, x2, y2, color, sim)

    def WriteData(self, hwnd, address, data, size):
        return _easyop.libop_WriteData(self, hwnd, address, data, size)

    def ReadData(self, hwnd, address, size):
        return _easyop.libop_ReadData(self, hwnd, address, size)

# Register libop in _easyop:
_easyop.libop_swigregister(libop)

